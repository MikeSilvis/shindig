require 'spec_helper'

describe "Attends" do
  # let(:user) { User.new }
  # let(:event) { Event.create(name: "Test", description: "Come Party!!!")}
  # before(:each) do
  #   ApplicationController.any_instance.stub(:require_login).and_return(true)
  # end
  # context "Adds event" do
  #   it "adds the event" do
  #     visit new_event_attend_path(event.id)
  #   end
  # end
end