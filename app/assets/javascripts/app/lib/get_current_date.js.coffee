class GetCurrentDate

  constructor: ->
  	# @d = Date.new()
  	@d = "HELLO MIKE"

  date: =>
		# @d.getFullYear() + "/" + (@d.getMonth() + 1) + "/" + @d.getDate()
		@d

window.GetCurrentDate = GetCurrentDate