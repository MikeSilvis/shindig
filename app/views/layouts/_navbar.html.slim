.navbar.navbar-fixed-top
  .navbar-inner
    .container
      a.brand= image_tag "shindigwithme-logo.png"
      .nav-collapse
        ul.nav.pull-right
          li(style="line-height:30px")
            - if current_user
              a= "signed in as: #{current_user.username}"
            - else
            	a(href="#" class='btn btn-info')  Log in
